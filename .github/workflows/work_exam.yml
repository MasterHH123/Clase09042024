name: Work Exam Horacio
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
      
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
          node-version: '14'

    - name: Print Information
      run: |
        echo "Horacio Hernandez"
        echo "Node is running version: $(node -v)"
        echo "Expediente es: 738176"
      

